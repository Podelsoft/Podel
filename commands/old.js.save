  const { createCanvas, loadImage } = require('canvas');
  const canvas = Canvas.createCanvas(360, 203);
  const ctx = canvas.getContext('2d');

  const { body: buffer } = await snekfetch.get(member.user.displayAvatarURL);
	const avatar = await Canvas.loadImage(buffer);
	 ctx.drawImage(avatar, 242, 60, 42, 42);
  
	// Since the image takes time to load, you should await it
	const background = await Canvas.loadImage("https://media.discordapp.net/attachments/556592360491057164/559523676782067712/xzjoin.png?width=360&height=203");
	// This uses the canvas dimensions to stretch the image onto the entire canvas
	ctx.drawImage(background, 0, 0, canvas.width, canvas.height);
  
	const NIGGAS = new Discord.Attachment(canvas.toBuffer(

   
  await message.channel.send(NIGGAS);
