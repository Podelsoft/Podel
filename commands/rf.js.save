const Discord = require('discord.js');

module.exports.run = async(bot, message, args) => {

if (message.guild.id !== "696515024746709003") return;

  let j = args.join(' '); let reason = j.split(args[0])[1];

  if (!reason) return message.channel.send('you must provide a valid 
  reason.')
  
  if (message.member.hasPermission("BAN_MEMBERS")) {
 
  let j = args.join(' ');

  let rolesearch = await message.guild.roles.find(role => role.name === j).members.map(m=>m.user.tag).join(', ');

  message.channel.send(rolesearch);  

 }

}

module.exports.help = {
   name: "rf",
   aliases: ["rolefind", "searchrole", "findrole"]
}
